'use strict';

/**
 * parking-img controller
*/
const { createCoreController } = require('@strapi/strapi').factories;
const { sanitizeEntity } = require('strapi-utils');

// @ts-ignore
module.exports = createCoreController('api::parking-img.parking-img', ({ strapi }) => ({
}));